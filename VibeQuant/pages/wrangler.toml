# Cloudflare Pages project — run deploy FROM this directory:
#   cd pages && npx wrangler pages deploy . --project-name=vibequant-web
#
# Pages does NOT support --config / custom wrangler path.
name = "vibequant-web"
compatibility_date = "2026-07-01"
pages_build_output_dir = "."
