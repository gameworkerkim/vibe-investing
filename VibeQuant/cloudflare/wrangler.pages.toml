# Cloudflare Pages — static dashboard (Pyodide UI)
# Deploy via: ./scripts/deploy.sh
#   or: npx wrangler pages deploy ../pages --config wrangler.pages.toml --project-name=vibequant-web
name = "vibequant-web"
compatibility_date = "2026-07-01"
pages_build_output_dir = "../pages"

# account_id filled by setup-secrets.sh / bootstrap.sh
# account_id = "YOUR_CLOUDFLARE_ACCOUNT_ID"
