{
  "name": "bithumb-arbitrage-signals",
  "version": "0.1.0",
  "private": true,
  "description": "빗썸(Bithumb) · 바이낸스(Binance) 차익거래 시그널 봇 — Cloudflare Workers + 정적 대시보드 + 텔레그램 알림",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
