{
  "name": "tokenforge",
  "version": "0.1.0",
  "private": true,
  "description": "TokenForge — 한국어·외국어 프롬프트를 DeepSeek으로 영어로 최적화하고, Claude·ChatGPT 토큰 절약을 예측하는 브라우저 대시보드 (Cloudflare Pages free tier)",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.129.0"
  }
}
